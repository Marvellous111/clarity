[project]
name = "clarity"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Marvellous", email = "marvellousnwachukwu111@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.28.1",
    "openai>=1.82.0",
    "python-dotenv>=1.1.0",
    "rich>=14.0.0",
]

[project.scripts]
clarity = "clarity.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
